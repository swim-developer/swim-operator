SWIM Kubernetes Operator installed successfully.

Operator deployment: {{ include "swim-kubernetes-operator.fullname" . }}
Namespace: {{ .Release.Namespace }}

Prerequisites (must be installed separately):
  - cert-manager (cert-manager.io)
  - Strimzi Kafka Operator (kafka.strimzi.io)
  - ActiveMQ Artemis Operator (broker.amq.io) — use upstream artemiscloud/activemq-artemis-operator
  - Prometheus Operator (monitoring.coreos.com) — optional, for ServiceMonitor support

Next steps:
  1. Verify the operator is running:
     kubectl get pods -n {{ .Release.Namespace }} -l control-plane=controller-manager

  2. Create a SWIM CR (example):
     kubectl apply -f config/samples/apps_v1alpha1_swimdigitalnotamconsumer.yaml

  3. Check operator logs:
     kubectl logs -n {{ .Release.Namespace }} -l control-plane=controller-manager -f
