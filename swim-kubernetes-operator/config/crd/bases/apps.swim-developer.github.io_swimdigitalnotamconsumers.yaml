---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: swimdigitalnotamconsumers.apps.swim-developer.github.io
spec:
  group: apps.swim-developer.github.io
  names:
    kind: SwimDigitalNotamConsumer
    listKind: SwimDigitalNotamConsumerList
    plural: swimdigitalnotamconsumers
    singular: swimdigitalnotamconsumer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              certManager:
                properties:
                  enabled:
                    default: true
                    type: boolean
                  issuerKind:
                    default: ClusterIssuer
                    type: string
                  issuerName:
                    default: swim-ca-issuer
                    type: string
                type: object
              client:
                properties:
                  config:
                    properties:
                      dnotamDeleteAndRecreate:
                        default: "true"
                        type: string
                      dnotamSubscriptions:
                        items:
                          properties:
                            airportHeliport:
                              items:
                                type: string
                              type: array
                            airspace:
                              items:
                                type: string
                              type: array
                            comment:
                              type: string
                            description:
                              type: string
                            eventScenario:
                              items:
                                type: string
                              type: array
                            eventSeries:
                              type: string
                            provider:
                              type: string
                            publisher:
                              type: string
                            queueName:
                              type: string
                            topic:
                              type: string
                          required:
                          - provider
                          - topic
                          type: object
                        type: array
                      observability:
                        properties:
                          openTelemetryEnabled:
                            default: false
                            type: boolean
                          otelEndpoint:
                            default: ""
                            type: string
                          otelHeaders:
                            default: ""
                            type: string
                          prometheusEnabled:
                            default: false
                            type: boolean
                          serviceMonitorEnabled:
                            default: false
                            type: boolean
                        type: object
                      providers:
                        items:
                          properties:
                            amqpBroker:
                              properties:
                                host:
                                  type: string
                                password:
                                  type: string
                                port:
                                  format: int32
                                  type: integer
                                sslEnabled:
                                  default: true
                                  type: boolean
                                tls:
                                  properties:
                                    keyStorePassword:
                                      type: string
                                    keyStorePath:
                                      type: string
                                    trustStorePassword:
                                      type: string
                                    trustStorePath:
                                      type: string
                                  type: object
                                username:
                                  type: string
                              required:
                              - host
                              - port
                              type: object
                            providerId:
                              type: string
                            subscriptionManager:
                              properties:
                                resilience:
                                  properties:
                                    connectTimeoutMs:
                                      default: 5000
                                      format: int32
                                      type: integer
                                    readTimeoutMs:
                                      default: 30000
                                      format: int32
                                      type: integer
                                    retryDelayMs:
                                      default: 1000
                                      format: int64
                                      type: integer
                                    retryMaxAttempts:
                                      default: 3
                                      format: int32
                                      type: integer
                                  type: object
                                tls:
                                  properties:
                                    keyStorePassword:
                                      type: string
                                    keyStorePath:
                                      type: string
                                    trustStorePassword:
                                      type: string
                                    trustStorePath:
                                      type: string
                                  type: object
                                url:
                                  type: string
                              required:
                              - url
                              type: object
                          required:
                          - amqpBroker
                          - providerId
                          - subscriptionManager
                          type: object
                        type: array
                      swimValidationEnabled:
                        default: "true"
                        type: string
                      swimValidationFailOnNullBody:
                        default: "false"
                        type: string
                    type: object
                  image:
                    default: quay.io/masales/swim-dnotam-consumer:latest
                    type: string
                  mongo:
                    properties:
                      database:
                        default: swim-dnotam
                        type: string
                      password:
                        default: swim
                        type: string
                      resources:
                        description: ResourceRequirements describes the compute resource
                          requirements.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      storageSize:
                        default: 1Gi
                        type: string
                      user:
                        default: swim
                        type: string
                    required:
                    - database
                    - password
                    - user
                    type: object
                  probe:
                    properties:
                      failureThreshold:
                        format: int32
                        type: integer
                      initialDelaySeconds:
                        format: int32
                        type: integer
                      periodSeconds:
                        format: int32
                        type: integer
                      timeoutSeconds:
                        format: int32
                        type: integer
                    type: object
                  replicas:
                    default: 1
                    format: int32
                    type: integer
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              global:
                properties:
                  clusterDomain:
                    default: ""
                    type: string
                type: object
              hpa:
                properties:
                  enabled:
                    type: boolean
                  maxReplicas:
                    default: 1
                    format: int32
                    type: integer
                  minReplicas:
                    default: 1
                    format: int32
                    type: integer
                  targetCPUUtilizationPercentage:
                    default: 80
                    format: int32
                    type: integer
                required:
                - maxReplicas
                type: object
              kafka:
                properties:
                  bootstrapServers:
                    type: string
                  deploymentMode:
                    default: managed
                    enum:
                    - managed
                    - external
                    type: string
                  enabled:
                    default: true
                    type: boolean
                  kafkaConsoleEnabled:
                    default: false
                    type: boolean
                  password:
                    type: string
                  replicas:
                    default: 1
                    format: int32
                    type: integer
                  storageSize:
                    default: 1Gi
                    type: string
                  username:
                    type: string
                  version:
                    default: 4.1.0
                    type: string
                type: object
                x-kubernetes-validations:
                - message: bootstrapServers is required when deploymentMode is external
                  rule: self.deploymentMode != 'external' || (has(self.bootstrapServers)
                    && size(self.bootstrapServers) > 0)
                - message: username is required when deploymentMode is external
                  rule: self.deploymentMode != 'external' || (has(self.username) &&
                    size(self.username) > 0)
                - message: password is required when deploymentMode is external
                  rule: self.deploymentMode != 'external' || (has(self.password) &&
                    size(self.password) > 0)
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
